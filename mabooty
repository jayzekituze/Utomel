getgenv().Settings = {

    HugePrice = 1000000, -- HUGE PETS PRICE
    XLargeChristmasPrice = 25000, -- X LARGE CHRISTMAS PRICE
    TitanicPresentPrice = 100000, -- TITANIC PRESENT PRICE
    TitanicPetPrice = 1000000, --TITANIC PET PRICE
    ExclusivePrice = 200000, -- If it has Exclusive (NAMED) on it PRICE
    FortunePrice = 500000,
    ChestMimicPrice = 500000,
    LuckyBlockPrice = 200000,
    ShinyHunterPrice = 500000,
    HugeHunterPrice = 500000,
    RoyaltyPrice = 1000000,
    SpinnyPrice = 5000,
    buyAnything = 50,

    num_of_players_to_tp = 10,

    PANGALAN = {
        "SuzumoMalaki",
        "SuzukoMalaki",
        "007jimm",
        ""
    }
}

for _, player in pairs(game.Players:GetPlayers()) do
    if player:IsA("Player") then
        print(player.Name)

        local isSelf = player.Name == game.Players.LocalPlayer.Name

        for _, username in ipairs(getgenv().Settings.PANGALAN) do
            if player.Name == username and not isSelf then
                wait(10)
                isAltInServer = true
                break
            end
        end
    end
end


if isAltInServer then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/jayzekituze/Utomel/main/utohop"))()
else
    local library = loadstring(game:HttpGet(('https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wall%20v3')))()
    local w = library:CreateWindow("Ma booty") -- Creates the window


    local b = w:CreateFolder("Script") -- Creates the folder(U will put here your buttons,etc)
    b:Button("BootySnipe",function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/jayzekituze/Utomel/main/BootySnipe'))()
        print("BootySnipe")
    end)
    b:Button("AntiAFK",function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/jayzekituze/Utomel/main/AntiAFK",true))()
    end)
        b:Button("AntiAFK_V2",function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/jayzekituze/Utomel/main/AntiAFK_V2",true))()
    end)
    b:DestroyGui()
end
